import 'package:flutter/material.dart';

class AddWaterResourcesScreen extends StatelessWidget {
  const AddWaterResourcesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
